local w = display.contentWidth
local h = display.contentHeight

local bg = display.pixelHeight

local data ={
      w = w,
      h = h,
      bgHeight = bg,
      center = w/2
}

return data

